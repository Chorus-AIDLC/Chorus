# add-claude-code-token-usage

Claude Code E2E per-turn token usage: capture from result envelope, upload via turn-advance, persist on DaemonSessionTurn + rollup on DaemonSession, display per-turn badge + conversation total
