# Tasks — Resource-Graph Node Search

## 1. Pure search + expand-to-reveal helpers
- [x] 1.1 Add `computeSearchMatches(nodes, query)` and `expandAncestorsForMatches(graph, matchIds)` (colocated with `resource-graph-visible-set.ts`). `computeSearchMatches` returns `Set<string>` of node ids whose lowercased title contains the lowercased trimmed query, computed over the caller-supplied (already type-filtered) nodes; a blank query returns `null` (not-searching) vs. an empty `Set` (zero hits). `expandAncestorsForMatches` returns `{ ideaUuids, proposalUuids }` resolving each match's ancestor chain (task/document → its proposal → that proposal's first project-local source idea; proposal → its first source idea; idea → none). Unit tests: case-insensitivity, substring-not-fuzzy, blank→null vs zero→empty Set, deep task-under-proposal-under-idea expansion, orphan/no-proposal nodes, and that match ordering follows pre-order outline order.

## 2. Search state + control UI in resource-graph.tsx
- [x] 2.1 Add search state to `resource-graph.tsx`: `searchQuery`, derived `matchIds` (over the existing type-filtered `visibleNodes`), `currentMatchIndex`, and the pre-search expand snapshot. Wire auto-expand by unioning `expandAncestorsForMatches` results into `expandedIdeas`/`expandedProposals` (add-only). Implement snapshot-on-enter / restore-on-clear (Q4): capture both Sets when query goes blank→non-blank, restore on cleared/Esc/blank. Pass `matchIds` and `currentMatchId` down to both renderers.
- [x] 2.2 Render the search controls on the existing top-right control `Card`: shadcn `<Input>` with search icon + clear (`X`) button, `Esc`-to-clear (routed through `isImeComposing` per the IME rule), the `current / total` count, a localized no-matches hint (Q2), and prev/next `<Button>`s with wrap-around index math (disabled when total is 0). All strings via `t()`; add `graph.search.*` keys to `en` + `zh`. Component test: typing updates count, clear/Esc resets + restores expand snapshot, prev/next wrap.

## 3. Canvas highlight, current-match ring, and camera centering
- [x] 3.1 Extend `mindmap-canvas.tsx` per-node alpha to compose hover/selection lineage with the match set (Q3): if `hoverId ?? selectedId` resolves a lineage, dim by lineage as today (hover takes over); else if a non-empty `matchIds` is active, match=1.0 / non-match=0.18; else everyone 1.0 (empty match set ⇒ no dim). Paint a distinct current-match ring on `currentMatchId` that does NOT set `selectedId`/`focusLineage`. Add a `centerNodeId` prop: when it changes, center the camera on that node using the `fitToView` centering math at the current scale (debounced recenter on query settle, per Q5/Q6). Component test: match dim vs. hover-takeover precedence, current-match ring presence, and that the cursor does not trigger lineage lighting.

## 4. Mobile outline highlight, count/nav, and scroll-into-view
- [x] 4.1 Add to `mindmap-outline.tsx` a match-highlight / non-match-dim row treatment (it has none today), the current-match accent, the same `current / total` count + prev/next controls, and `scrollIntoView({ block: "center" })` for the current match via a per-row ref. Reads the same shared search state so a viewport resize preserves the query/matches/cursor. Component test: matching rows highlighted, non-matching dimmed, current row scrolled into view, count + wrap-around nav.

## 5. Integration verification + design.pen
- [x] 5.1 End-to-end verification across the search feature with both renderers wired to the shared state: searching auto-expands to a deep match and dims the rest; hovering during search takes over with full lineage then returns to match highlight on mouse-out; prev/next steps and centers (canvas) / scrolls (outline) with wrap-around; clearing/Esc restores the exact pre-search expand layout; an empty result shows the no-matches hint with stepping disabled and no whole-tree dim; the type filter excludes filtered-out types from matches without flipping checkboxes. Run `pnpm lint`, `npx tsc --noEmit`, and `pnpm test` green. Update `docs/design.pen` ("Chorus - Project Graph View") to show the search box, count/nav, match highlight, current-match ring, and dimmed non-matches.
