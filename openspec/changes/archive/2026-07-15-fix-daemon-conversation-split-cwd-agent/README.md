# fix-daemon-conversation-split-cwd-agent

Re-point the idea's canonical daemon conversation on cwd/agent switch instead of forking a residual thread, and harden interrupt to reach the idea's running turn from any thread
