{
  "mcpServers": {
    "chorus": {
      "type": "http",
      "url": "${user_config.CHORUS_URL}/api/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.CHORUS_API_KEY}"
      }
    }
  }
}
